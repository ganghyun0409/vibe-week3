# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

This repository (`vibe-week3`) is currently empty aside from a placeholder `README.md`. There is no source code, build system, test suite, or dependency manifest yet.

There are no build, lint, or test commands to document because none exist yet. When code is added to this repository, this file should be updated with:
- The language/framework chosen and how to install dependencies
- Commands to build, lint, and run tests (including running a single test)
- The high-level architecture once there is more than one file to describe

## Communication

- 모든 대화(응답)는 한국어로 작성한다.
- 모든 git 커밋 메시지는 한국어로 작성한다.
